// פינים של חיישן אולטרסוני
const int trigPin = 7;   // פין שליחת פולס
const int echoPin = 6;   // פין קבלת ההחזר

// פינים של נורת RGB
const int redPin = 9;    // אדום
const int greenPin = 10; // ירוק
const int bluePin = 11;  // כחול

// משתנים למדידה
long duration;   // משך הזמן של הפולס החוזר
long x;          // ערך של המרחק (שלם)
float distance;  // המרחק בסנטימטרים

void setup() {
  // הגדרת כיווני הפינים של החיישן
  pinMode(trigPin, OUTPUT);
  pinMode(echoPin, INPUT);

  // הגדרת פיני ה־RGB כפלט
  pinMode(redPin, OUTPUT);
  pinMode(greenPin, OUTPUT);
  pinMode(bluePin, OUTPUT);

  // ודא שה־trig כבוי בהתחלה
  digitalWrite(trigPin, LOW);

  // פתיחת תקשורת סריאלית
  Serial.begin(9600);
}

// פונקציה שמכבה את כל הצבעים ב־RGB
void ledOff() {
  analogWrite(redPin, 0);
  analogWrite(greenPin, 0);
  analogWrite(bluePin, 0);
}

void loop() {
  // שליחת פולס אולטרסוני קצר
  digitalWrite(trigPin, LO
